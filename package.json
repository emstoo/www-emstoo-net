{
  "name": "www.emstoo.net",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "content:push": "for f in $(find content -name '*.md'); do wrangler r2 object put \"www-emstoo-net-content/$f\" --file=\"$f\"; done",
    "content:push:local": "for f in $(find content -name '*.md'); do wrangler r2 object put \"www-emstoo-net-content/$f\" --file=\"$f\" --local; done",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings"
  },
  "dependencies": {
    "hono": "^4.10.6",
    "marked": "^18.0.5"
  },
  "devDependencies": {
    "wrangler": "^4.4.0"
  }
}
